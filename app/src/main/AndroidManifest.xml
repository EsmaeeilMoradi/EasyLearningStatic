<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esmaeeil_moradi.easy_learning_static">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OrderActivity"
            android:label="@string/create_order"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.esmaeeil_moradi.easy_learning_static.MainActivity" />
        </activity>
        <activity
            android:name=".ChapterDetailActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".converter.ConverterActivity" />
        <activity android:name=".equilibrium.Equilibrium2DActivity" />
        <activity android:name=".analysisOfBeam.AnalysisOfBeamActivity"></activity>
    </application>

</manifest>